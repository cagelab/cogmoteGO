info:
  name: Update Git experiment
  type: http
  seq: 27
  tags:
    - manual

http:
  method: PUT
  url: "{{baseUrl}}/exps/:id/git"
  params:
    - name: id
      value: "{{experimentId}}"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Pulls updates for an initialized Git experiment.
