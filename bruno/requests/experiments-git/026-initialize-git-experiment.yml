info:
  name: Initialize Git experiment
  type: http
  seq: 26
  tags:
    - manual

http:
  method: POST
  url: "{{baseUrl}}/exps/:id/git"
  params:
    - name: id
      value: "{{experimentId}}"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Clones and initializes a registered experiment whose type is git.
