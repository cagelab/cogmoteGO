info:
  name: Get email configuration
  type: http
  seq: 44
  tags:
    - regression

http:
  method: GET
  url: "{{baseUrl}}/email/config"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Returns non-secret SMTP configuration and whether credentials are available.
