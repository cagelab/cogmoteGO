info:
  name: Get experiment status
  type: http
  seq: 35
  tags:
    - smoke

http:
  method: GET
  url: "{{baseUrl}}/status"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Returns the global in-memory experiment running status.
