info:
  name: Stop experiment
  type: http
  seq: 33
  tags:
    - manual

http:
  method: POST
  url: "{{baseUrl}}/exps/:id/stop"
  params:
    - name: id
      value: "{{experimentId}}"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Kills the currently running experiment process.
