info:
  name: List experiments
  type: http
  seq: 20
  tags:
    - smoke

http:
  method: GET
  url: "{{baseUrl}}/exps"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Lists all registered experiment records.
