info:
  name: Delete experiment
  type: http
  seq: 25
  tags:
    - destructive
    - manual

http:
  method: DELETE
  url: "{{baseUrl}}/exps/:id"
  params:
    - name: id
      value: "{{experimentId}}"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Deletes one experiment record.
